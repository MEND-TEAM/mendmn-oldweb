== CMS - Strapi headless CMS api framework ==

== Setup  ==

npm install strapi@beta -g

##############
npm install                 = it install all node modules in your project, admin and plugins

npm run develop             = start strapi by developer mode